# RLY-20260722-012 Backend Rework Report

- Agent: `codex-backend-20260722T113200Z-50602`
- Role: Backend writer
- Reworked implementation: `80ba34c7e0af3a8940026b314d3f9bd0b2e11cbf`

## Rework completed

- Group deletion now tombstones active `group_members` and `section_groups` rows in the existing deletion transaction, in addition to clearing `primary_group_id`. This makes access revocation and ACL tombstone sync immediate and atomic.
- Section metadata sync now enforces D3: manager+ receives oversight metadata, while editor/member receives public Sections and private Sections granted through active group membership only.
- Grant hydration now backfills messages in the configured messaging window for newly accessible Sections, plus their attachments, embeds, reactions, and mentions.
- Core Section access and sync predicates also reject soft-deleted groups.

## Verification

- All packet-listed `node --check` commands passed.
- Additional `node --check` passed for `src/daos/syncDAO.js` and `src/services/syncService.js`.
- `git diff --check` passed.

## Verification limits

- No database integration test was run because this worktree has no approved disposable PostgreSQL database or migration runner.
- The repository `npm test` command is the documented failing placeholder and was not treated as verification.
