# RLY-20260722-012 Backend Rework Report

- Agent: `codex-backend-20260722T141754Z-71790`
- Role: Backend writer
- Base: `b6ef445`
- Active packet: spec revision 2, Section→Group 1:N

## Implemented

- Replaced the obsolete `section_groups` join table with nullable `sections.group_id`, its partial index, and reversible migration SQL.
- Reworked Section create/update and access queries for the scalar group contract, including transactional private creation/update, grant-0 rejection, default-Section protection, manager metadata oversight, and group-membership-only content access.
- Removed the obsolete Section group connect/disconnect endpoints and payload handling.
- Updated group deletion to leave private Sections in the approved `group_id=NULL` 0-member-lock state and retained group-member/primary-label cleanup.
- Removed `section_groups` from sync payloads and changed sync, notification, attachment, search, event, and task Section queries to the scalar access path.
- Added the required group member-list route and retained manager+ mutation authorization.

## Verification

- All packet-required `node --check` commands passed.
- Additional syntax checks passed for modified sync and authorization files.
- `git diff --check` passed.
- Residual scan found no runtime `section_groups` or `group_ids` references.

## Verification limits

- No disposable PostgreSQL database was provided, so migration and concurrency behavior were not integration-tested.
- `npm test` remains the repository's documented failing placeholder and was not treated as verification.
