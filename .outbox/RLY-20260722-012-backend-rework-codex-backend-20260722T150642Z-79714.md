# RLY-20260722-012 Backend Rework Report

- Agent: `codex-backend-20260722T150642Z-79714`
- Role: Backend writer
- Packet: spec revision 2
- Rework source: implementation review of `a0f1f2e54cc37da791dc2212811f732678423ab9`

## Implemented

- Added an explicit migration guard that rejects legacy `sections.access_scope` values outside the approved 0/1 domain before installing the two-value CHECK constraint. No unapproved mapping of legacy private data is inferred.
- Changed master transfer to lock and re-read the actor and target `binder_members` rows with `SELECT ... FOR UPDATE` inside the transaction before authorization and mutation.
- Enforced the role-mutation ordering rule for master transfer and rejected self-transfer, preventing concurrent transfers from leaving multiple masters or self-transfer from leaving no master.

## Verification

- All six packet-required `node --check` commands passed.
- Additional `node --check` passed for `src/services/binderService.js` and `src/daos/binderDAO.js`.
- `git diff --check` passed.
- Residual scan found no `section_groups` or `group_ids` references under `src/` or `migrations/`.

## Verification limits

- A disposable PostgreSQL database was not provided, so migration execution and concurrent transaction behavior were not integration-tested against PostgreSQL.
- `npm test` is the repository's documented failing placeholder and is not treated as successful verification.
