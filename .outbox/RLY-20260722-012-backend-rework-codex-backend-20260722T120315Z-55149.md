# RLY-20260722-012 Backend Rework Report

- Agent: `codex-backend-20260722T120315Z-55149`
- Role: Backend writer
- Reviewed implementation: `852bcda73747da14fe666540cb0261d954dccf0d`

## Rework completed

- Binder role changes and kicks now lock the actor and target membership rows in one transaction and enforce `actor.role < target.role`; role changes additionally enforce `actor.role < target.new_role`.
- Group grants on the default Section are rejected before mutation.
- `GroupService.getMembers` now wraps `GroupDAO.getMembers` with group existence and manager authorization checks.
- Added directly executable rollback SQL at `migrations/20260722_group_section_access.down.sql`.

## Verification

- All task-packet `node --check` commands passed.
- Additional `node --check` passed for `src/services/binderService.js` and `src/daos/binderDAO.js`.
- `git diff --check` passed.

## Verification limits

- Database integration tests were not run because no approved disposable PostgreSQL target or migration runner is available in this worktree.
- No focused ACL test harness exists in the repository; the documented `npm test` command is a failing placeholder and was not treated as verification.
