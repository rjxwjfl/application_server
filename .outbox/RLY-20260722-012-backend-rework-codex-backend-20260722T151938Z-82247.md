# RLY-20260722-012 Backend Rework Report

- Role: backend
- Agent: `codex-backend-20260722T151938Z-82247`
- Base reviewed SHA: `adc29def3d29ca5c25c5aa4c80f743f1d3233875`
- Status: succeeded

## Changes

- Normalized every public Section PATCH (`access_scope=0`) to persist `group_id=NULL`, including requests that supply a group and public transitions that previously retained a stale group.
- Required an explicit non-null `group_id` when PATCH changes a public Section to private.
- Added PATCH validation that rejects `access_scope` values outside `0` and `1`.
- Added DAO-level public-scope normalization as a defense-in-depth persistence invariant.

## Files

- `src/services/sectionService.js`
- `src/daos/sectionDAO.js`

## Verification

- `git diff --check` passed.
- All six task-packet `node --check` commands passed.
- `rg -n "section_groups|sections/:sectionId/groups|group_ids" src migrations` returned no matches.
- `npm test` was not treated as verification because the repository script is a known failing placeholder.

## Scope

- No dependency, schema, endpoint, standard, control-repository, or out-of-scope changes were made.
- Existing untracked bridge runtime artifacts under `.outbox/runtime/` were preserved.
